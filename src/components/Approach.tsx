export default function Approach() {
  return (
    <section id="approach" className="py-24 px-6 scroll-mt-16">
      <div className="max-w-3xl mx-auto text-center">
        <h2 className="text-3xl md:text-4xl font-bold mb-8">My approach</h2>
        <p className="text-lg text-brand-gray leading-relaxed">
          I build custom websites for Australian businesses using a modern toolchain — no templates,
          no plugin sprawl. The result is a site that loads fast, looks distinctive, and behaves
          exactly the way you want it to. Most projects launch in weeks, not months.
        </p>
      </div>
    </section>
  )
}
